# 라이브 마스터 — 현재 상태 (2026-08-12 기준)

이 문서는 작업 이어받기용 요약입니다. 세부 이유는 각 커밋 메시지에 자세히 적혀 있습니다.

## 배포 구조

```
컨트롤러/오버레이 (브라우저)
        │ SSE (/api/stream)
        ▼
Render 웹서비스 live-master-server (오레곤, GitHub main 자동배포)
        │
        ├─ Supabase Postgres (서울)  ← 방송 상태·장부·시그니처 메타
        └─ Supabase Storage (media)  ← 시그니처 사진/음원 (공개 버킷, 1년 캐시)
```

- 프로덕션: https://live-master-server.onrender.com
- 저장소: https://github.com/Phil0508/live-master-server
- OBS 브라우저 소스: `/overlay` (1080×1920). 확인용으로 브라우저에 띄울 땐 `/overlay?monitor=1`
  (소리 중복 재생·큐 이중 소모 방지)
- Render 환경변수: `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_SECRET_KEY`
- 접속정보 전체: `SUPABASE_CREDENTIALS.txt` (gitignore됨)

## 완료된 작업

**시그니처 시스템 (Supabase 이전)**
- 로컬 8388 프로그램 의존성 제거. 시그니처 99개, 미디어 94+94개가 Supabase Storage에 있음
- 사진 WebP 압축으로 254MB → 8.4MB
- 컨트롤러 `시그니처` 탭: 등록/편집/삭제/검색/송출
- 금액 매칭: **정확히 일치 → 없으면 올림 → 그래도 없으면 최고가**
- 수동 송출 시 `정산 장부에 기록` 체크박스 (기본 꺼짐)

**슬롯머신**
- 방송마다 후보 20개 선택 (`slot_pool`, 랜덤20/전체선택/해제 버튼)
- 릴에 시그니처 사진 표시. 릴 크기 170×120은 정지 좌표와 묶여 있어 **변경 금지**
- 당첨 → 슬롯 위젯 자동 OFF → 리액션 큐 투입 → 재생 종료 시 리액션 모드 자동 해제

**데이터 영속성**
- `DATABASE_URL` 미설정으로 재시작마다 전부 소실되던 문제 해결
- 상태 저장이 실제로 동작하지 않던 버그 수정 (같은 객체를 비교해 "변경 없음" 판정하던 문제 → `LAST_PERSISTED`와 비교)
- 방송 시작/종료가 장부를 백업 없이 삭제하던 문제 → `donation_archive`에 영구 보관

**예전 vercel 저장소에서 이식한 기능**
- 은행 원장 `bank_ledger`: 점수 변동을 통장처럼 기록. 시스템 탭에서 조회 + `잔액 재정산`으로 복구
- 퇴근전쟁(퇴근빵): 랭킹 탭 스위치 + 플레이어별 목표 → 진행바 → 승인대기함 카드 → 송출 시 축하 팝업
- VIP 특별 후원자: 닉네임별 등급/색/뱃지 → 후원 팝업이 등급 색으로
- 피버 타임(대결 60초 이하 자동 불꽃), 목표 100% 달성 연출(게이지 중앙 확대)

**디자인 리뉴얼**
- `overlay.html` `:root`에 디자인 토큰. 시즌 테마는 여기 값만 바꾸면 됨
- 정보 위젯(랭킹·계좌·게이지·팝업) = 유리 카드. 랭킹판은 선으로 칸 나눈 표 유지
- 게임 위젯(대결·슬롯) = **배경 판 없이** 요소만 화면 위에 뜸 (그림자로 가독성 확보)
- 투명 배경: 다크모드에서 캔버스가 어둡게 칠해지는 문제 → `color-scheme: only light`

**성능**
- 점수 입력 2.4초 → 0.3초. 원인 3가지: 동기 저장 / 매번 새 DB 연결 / 행마다 개별 쿼리
- 스레드별 연결 재사용, 다중행 일괄 저장, 점수 변경은 비동기 큐

## 알려진 미해결 항목

- **사진 또는 음원이 없는 시그니처 10개** — 컨트롤러 시그니처 탭에서 편집으로 채우면 됨
  (사운드 없음: 탕탕탕/LSD/10500·600000·700000원 · 사진 없음: 30002·40003·51000·100009·2000000원)
- **Vercel 잔재** `vercel.json`, `api/index.py` — 현재 코드는 서버리스에서 못 돌아감. 정리 대상
- **Supabase 무료 전송량 5GB/월** — 캐시 적용 후 월 수백 MB 예상이나 가끔 확인 권장

## 다음에 하려던 것

**조종실(운영비) 후원 전용 이펙트** — 방향만 제안한 상태, 사용자 확답 대기 중.
현재 운영비(`bottom_fixed`)는 숫자만 조용히 올라가고 연출이 없음. 제안했던 안:
- 전용 후원 팝업(청록/민트 + "조종실에 커피를 쏘셨습니다 ☕")
- 하단 감사 배너 슬라이드
- 운영비 줄 반짝 연출
- 운영비 전용 미니 게이지

먼저 확인이 필요했던 것: 운영비 후원이 **승인대기함에서 수동 지급**인지, **특정 금액 자동 구분**인지, **아직 방식이 없는지**.

## 작업 시 주의사항

- 테스트로 프로덕션/Supabase에 데이터를 넣었으면 **반드시 정리**할 것 (players, donation_history,
  snapshots, kv_store, donation_archive, bank_ledger, vip_donators). `signatures` 테이블은 건드리지 말 것
- 로컬 서버 테스트 시 `localhost` 대신 **`127.0.0.1`** 사용 (IPv6 폴백으로 2초 지연 측정 오류 발생)
- 브라우저 자동화로 애니메이션 검증 시, 백그라운드 탭은 트랜지션이 멈춤 →
  `transition:'none'` 걸고 값만 확인할 것
- `load_data()`는 `DEFAULT_STATE`에 있는 키만 복원함. 새 상태 키는 반드시 거기 추가
- Postgres는 트랜잭션 안에서 한 문장이 실패하면 전체가 롤백됨 (`ALTER TABLE`은 별도 연결에서)
